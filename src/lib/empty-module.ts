// Intentionally empty. Used as a browser stub for Node built-ins (fs, path,
// crypto) that the qpdf Emscripten glue references behind environment checks
// but never actually calls in the browser.
export default {};
